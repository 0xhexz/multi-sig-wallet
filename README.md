# Multi-Sig Wallet

A simple multi-signature wallet requiring multiple owners to approve a transaction.

## Features
- Submit transaction
- Approve transaction
- Execute transaction when approval threshold is met

## Run locally
1. npm install
2. npx hardhat node
3. npx hardhat run scripts/deploy.js --network localhost
